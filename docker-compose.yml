services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: bulcoin
      POSTGRES_PASSWORD: bulcoin
      POSTGRES_DB: bulcoin
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
